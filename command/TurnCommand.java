package command;

import menu.Menu;

public class TurnCommand implements Command {

    @Override
    public Menu execute(){
        // check if all players have filled their cards to declare a winner

        return null;
    }
}
